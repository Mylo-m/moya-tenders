# Open Questions — decisions needed from owner

1. **Cloud Run deploy** — create GCP project + billing, give me the key? This is
   the only hard judging requirement still missing.
2. **Gemini API key** — paste into `C:\Users\ordio\.env` (`GEMINI_API_KEY=`).
3. **EG / MZ / BW** tenders — build real scrapers, or accept empty?
4. **Live dashboard** — keep current fix, or also publish the `Moya/` cyber SPA
   as a second view?
5. **WITS proposal** — confirm which PDF is the deliverable.
6. **Obsidian/VPS second-brain** — video suggests syncing vault to a VPS so the
   agent runs 24/7 + you edit skills visually. Needs a VPS + Obsidian Sync sub.
   Optional; not required for the hackathon.
